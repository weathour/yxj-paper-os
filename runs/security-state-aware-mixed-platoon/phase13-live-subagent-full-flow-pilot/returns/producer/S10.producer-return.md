## Candidate material / Assessment

Packet citation: `packets/producer/S10.producer-packet.json`. For stage S10 — Main-text production — the candidate material is appropriate as a production-candidate report, not as final manuscript output. S10’s role is to turn bounded task-packet inputs into candidate text units while preserving controller-owned completion. The packet correctly consumes the S09B task packet, construction matrix, terminology register, claim visibility, and local manuscript anchors: `manuscript/sections/01_introduction.tex`, `04_method.tex`, and `06_results_discussion.tex`.

The local paper spine is clear enough for main-text production: the active method is `zt_md_mpc_full` / Zero-Trust Memory-Degradation MPC; “zero trust” is bounded to controller-side zero-default authority for degraded V2X evidence, not cryptographic or network zero trust; and the evidence spine is the 20260625 method-faithful validation package.

## Actual effect

S10 can produce bounded candidate prose that connects the paper’s main argument across introduction, method, and results. The likely effective text function is to keep the manuscript’s through-line consistent: mixed-platoon control is framed as an information-authority problem; ZT-MD-MPC implements four-channel authority allocation among fresh V2X, short trusted memory, local sensing, and physical dynamics; results support an aggregate tradeoff claim over 17 methods, 9 scenarios, and 200 held-out validation seeds per scenario.

The stage also helps enforce claim discipline. Candidate text should distinguish the conservative in-suite comparator claim from the broader common-suite external mechanism panel, preserve the ACC-only raw gap/TTC boundary, and avoid converting deterministic analytic stress-suite evidence into field, HIL, deployment, stability-proof, or publication-readiness claims.

## Limitations

S10 remains a producer-lane candidate stage. It does not validate final content, mark graph completion, or authorize owner/script/controller outputs. The packet is production_candidate, but controller-owned completion and worker-completion-forbidden boundaries still apply. Any generated main-text unit would need downstream validation against the content validator `phase10_s10_content_validator`, the section draft validator, candidate return validator, and claim boundary check.

The main writing risk is overclaiming. Strong numeric results can tempt language that implies universal safety or deployment readiness. S10 should instead maintain the bounded phrasing: collision-free within the validation suite, aggregate rank-1 under the predefined score, ACC-only leading raw safety-margin axes, and external mechanism rows treated as common-suite stress comparators rather than exact source-paper reproductions.

## Downstream usefulness

This S10 report is useful downstream as a writing boundary map. It tells later controller or verifier stages what a compliant main-text candidate should do: preserve terminology, carry the authority-allocation spine, align text with the construction matrix and claim visibility, and route unresolved sufficiency questions to validation rather than prose inflation. It also gives S15 repair/regeneration a narrow target if backflow is needed: repair only affected text units and their local packets, not the entire manuscript route.

## Boundary statement

This report is limited to the Phase13 live subagent pilot for S10 — Main-text production. It does not edit files, dispatch agents, revive `$yxj-paper-os` or `$yxj-plugin-incubator` as active routes, claim controller completion authority, or claim manuscript, submission, or publication readiness.
